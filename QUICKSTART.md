# Быстрый старт Нейродиректолог

## 1. Установка зависимостей

```bash
# Установите все зависимости проекта
npm install
```

## 2. Настройка переменных окружения

### Backend

Создайте файл `server/.env`:

```env
# Database (можете использовать любую PostgreSQL БД или SQLite для тестирования)
DATABASE_URL="postgresql://user:password@localhost:5432/neurodirectolog?schema=public"

# Server
PORT=3001
NODE_ENV=development

# JWT Secret (замените на свой секретный ключ)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# OpenAI API (получите на https://platform.openai.com/api-keys)
OPENAI_API_KEY=your-openai-api-key-here

# Yandex Direct API (получите в настройках Яндекс.Директ)
YANDEX_DIRECT_TOKEN=your-yandex-direct-token-here
YANDEX_CLIENT_LOGIN=your-client-login

# Cors
CORS_ORIGIN=http://localhost:5173
```

### Frontend

Создайте файл `client/.env`:

```env
VITE_API_URL=http://localhost:3001/api
```

## 3. Запуск проекта

### Простой способ (один терминал)

```bash
npm run dev
```

Это запустит одновременно:
- Backend на http://localhost:3001
- Frontend на http://localhost:5173

### Раздельный запуск (два терминала)

**Терминал 1 - Backend:**
```bash
npm run dev:server
```

**Терминал 2 - Frontend:**
```bash
npm run dev:client
```

## 4. Первый запуск

1. Откройте браузер и перейдите на http://localhost:5173
2. Нажмите "Зарегистрироваться"
3. Создайте аккаунт
4. Войдите в систему
5. Начните работу!

## 5. Тестирование функционала

### Сбор семантики
1. Перейдите в раздел "Семантика"
2. Введите нишу: "Доставка еды"
3. Описание: "Служба доставки готовой еды с собственной кухней. Быстрая доставка в течение 30 минут."
4. Нажмите "Сгенерировать семантику"
5. Дождитесь результата и экспортируйте в Excel

### Генерация объявлений
1. Перейдите в раздел "Объявления"
2. Введите ключевые слова (по одному на строку)
3. Опишите ваш бизнес
4. Укажите УТП (например: "Бесплатная доставка от 500 руб")
5. Нажмите "Заголовки" или "Тексты"

## Важные замечания

### API ключи
- Без API ключа OpenAI функционал генерации работать не будет
- API ключ Яндекс.Директ нужен только для создания кампаний напрямую в Директе
- Экспорт в Excel/CSV работает без API ключей Яндекса

### База данных
- Если у вас нет PostgreSQL, проект работает и без БД (данные пользователей хранятся временно в памяти)
- Для продакшена обязательно настройте PostgreSQL

### Стоимость
- OpenAI API платный, стоимость зависит от использования
- Для тестирования достаточно $5-10 на счету OpenAI

## Структура проекта

```
Neurodirectolog/
├── client/          # Frontend (React + Vite)
├── server/          # Backend (Node.js + Express)
├── package.json     # Корневой package.json
└── README.md        # Полная документация
```

## Решение проблем

### Ошибка подключения к API
- Проверьте, что backend запущен на порту 3001
- Убедитесь, что в `client/.env` указан правильный URL API

### Ошибка OpenAI API
- Проверьте правильность API ключа
- Убедитесь, что на счету OpenAI есть средства
- Проверьте лимиты вашего аккаунта

### Порты заняты
Если порты 3001 или 5173 заняты, измените их:
- Backend: в `server/.env` измените `PORT`
- Frontend: при запуске Vite укажет альтернативный порт автоматически

## Поддержка

Если возникли вопросы - смотрите полную документацию в [README.md](README.md)

---

Удачи в работе с Нейродиректологом! 🚀
