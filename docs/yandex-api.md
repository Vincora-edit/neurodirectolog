# Дашборд аналитики Яндекс.Директ

## Что реализовано

Полноценная система аналитики для Яндекс.Директ с красивым UI и AI-анализом:

### Backend:
- ✅ OAuth авторизация с Яндекс.Директ
- ✅ Автоматическая синхронизация данных каждые 30 минут
- ✅ Интеграция с Яндекс.Метрикой для конверсий
- ✅ ClickHouse для хранения больших объемов данных
- ✅ AI-анализ кампаний с GPT-4o
- ✅ Автоматическая генерация рекомендаций

### Frontend:
- ✅ Красивый дашборд с градиентными карточками
- ✅ Таблица кампаний с цветовой индикацией
- ✅ Визуальные индикаторы производительности (↑ ↓)
- ✅ Выбор периода (7/30/90 дней)
- ✅ Ручная синхронизация

### Database (ClickHouse):
- ✅ 13 таблиц для хранения всех данных
- ✅ Материализованные представления для быстрой аналитики
- ✅ Партиционирование по месяцам

## Текущий статус

### ✅ Работает:
- Docker контейнер с ClickHouse запущен и работает
- База данных `neurodirectolog` создана
- Все таблицы созданы и готовы к работе
- Backend сервер запущен на http://localhost:3001
- Frontend запущен на http://localhost:5173
- Cron job для автоматической синхронизации активен

### ⚠️ Требуется настройка:

Для полноценной работы нужно получить OAuth credentials от Яндекса:

1. **Создать приложение в Яндекс.OAuth:**
   - Перейти на https://oauth.yandex.ru/
   - Создать новое приложение
   - Указать callback URL: `http://localhost:5173/yandex/callback`
   - Получить Client ID и Client Secret

2. **Обновить .env файл:**
```bash
YANDEX_CLIENT_ID=ваш-client-id
YANDEX_CLIENT_SECRET=ваш-client-secret
```

3. **Перезапустить сервер** (он автоматически перезапустится при изменении .env)

## Как использовать

### 1. Первое подключение:

1. Перейти на `/connect-yandex` в браузере
2. Нажать "Подключить Яндекс.Директ"
3. Авторизоваться в Яндекс и дать разрешение на доступ
4. Выбрать проект для подключения
5. Опционально: указать счетчик Метрики и выбрать цели

### 2. Просмотр аналитики:

1. Перейти на `/yandex-dashboard`
2. Увидите:
   - **Ключевые метрики** (Расход, Клики, Конверсии, ROI)
   - **Таблица кампаний** с полной статистикой
   - **Период** можно выбрать (7/30/90 дней)
   - **Кнопка обновить** для ручной синхронизации

### 3. AI рекомендации:

После синхронизации система автоматически:
- Анализирует все кампании
- Выявляет проблемы (низкий CTR, нет конверсий, высокий CPC)
- Генерирует рекомендации через GPT-4o
- Сохраняет их в базу данных

## Архитектура

```
Client (React)
    ↓ HTTP
Backend (Node.js + Express)
    ↓
    ├─→ Yandex.Direct API (кампании, статистика)
    ├─→ Yandex.Metrika API (конверсии)
    ├─→ OpenAI GPT-4o (AI анализ)
    └─→ ClickHouse (хранилище данных)
         ↓
         ├─ yandex_direct_connections
         ├─ campaigns
         ├─ campaign_stats (партиционирована по месяцам)
         ├─ ai_recommendations
         └─ materialized views для быстрой аналитики
```

## Структура данных

### Основные таблицы:

1. **yandex_direct_connections** - подключения к Яндекс.Директ
2. **campaigns** - список кампаний
3. **campaign_stats** - статистика по дням (партиционирована)
4. **ai_recommendations** - рекомендации от AI
5. **change_history** - лог изменений

### Метрики:

- Показы (impressions)
- Клики (clicks)
- CTR (click-through rate)
- CPC (cost per click)
- CPM (cost per mille)
- Конверсии (conversions)
- CR (conversion rate)
- Стоимость конверсии
- Квалифицированные лиды
- ROI (return on investment)

## Cron Job

Автоматическая синхронизация запускается **каждые 30 минут**:

1. Получает обновленный список кампаний
2. Загружает статистику за последние 7 дней
3. Получает конверсии из Метрики (если настроена)
4. Вычисляет все метрики
5. Сохраняет в ClickHouse
6. Запускает AI-анализ
7. Генерирует рекомендации

## Примеры AI рекомендаций

Система автоматически выявляет:

### 🔴 Критические проблемы:
- Нет конверсий при большом количестве кликов
- Отрицательный ROI (убыточная кампания)

### ⚠️ Предупреждения:
- Низкий CTR (< 2%)
- Высокий CPC (> 100 руб)
- Низкая конверсия (< 2%)

### 💡 Рекомендации:
- Пересмотреть тексты объявлений
- Добавить минус-слова
- Снизить ставки
- Оптимизировать посадочную страницу
- Проверить настройки целей

## Endpoints API

### Yandex.Direct:
- `GET /api/yandex/auth-url` - получить URL для OAuth
- `POST /api/yandex/connect` - завершить подключение
- `GET /api/yandex/connection/:projectId` - информация о подключении
- `GET /api/yandex/campaigns/:projectId` - список кампаний
- `GET /api/yandex/stats/:projectId` - агрегированная статистика
- `GET /api/yandex/campaign-stats/:campaignId` - детальная статистика
- `POST /api/yandex/sync/:projectId` - ручная синхронизация

### Yandex.Metrika:
- `GET /api/yandex/metrika/goals/:counterId` - получить цели счетчика

## Проверка работы ClickHouse

```bash
# Показать все таблицы
docker exec yd-clickhouse clickhouse-client --database=neurodirectolog --query "SHOW TABLES"

# Показать количество записей в campaign_stats
docker exec yd-clickhouse clickhouse-client --database=neurodirectolog --query "SELECT count() FROM campaign_stats"

# Показать последние подключения
docker exec yd-clickhouse clickhouse-client --database=neurodirectolog --query "SELECT * FROM yandex_direct_connections LIMIT 5"

# Показать AI рекомендации
docker exec yd-clickhouse clickhouse-client --database=neurodirectolog --query "SELECT * FROM ai_recommendations LIMIT 10"
```

## Известные особенности

1. **Предупреждение о `host` deprecated** - это warning от ClickHouse клиента, не критично
2. **OAuth credentials пустые** - нужно получить от Яндекса и добавить в .env
3. **Материализованные view** - обновляются автоматически при вставке данных
4. **Партиционирование** - данные автоматически распределяются по месяцам для производительности

## Что дальше

Для production развертывания:

1. ✅ Получить OAuth credentials от Яндекса
2. ✅ Настроить production базу данных ClickHouse
3. ✅ Настроить HTTPS для callback URL
4. ⚠️ Добавить отображение AI рекомендаций на дашборде (сейчас заглушка)
5. ⚠️ Добавить фильтры по кампаниям
6. ⚠️ Добавить графики динамики метрик
7. ⚠️ Добавить экспорт данных в Excel
8. ⚠️ Интеграция с CRM для отслеживания revenue

## Полезные команды

```bash
# Проверить статус ClickHouse
docker ps | grep clickhouse

# Остановить ClickHouse
docker stop yd-clickhouse

# Запустить ClickHouse
docker start yd-clickhouse

# Посмотреть логи ClickHouse
docker logs yd-clickhouse

# Подключиться к ClickHouse CLI
docker exec -it yd-clickhouse clickhouse-client --database=neurodirectolog
```

## Контакты для поддержки

Все вопросы и проблемы можно обсуждать в Issues GitHub репозитория.
