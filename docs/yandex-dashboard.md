# Быстрый старт - Дашборд Яндекс.Директ

## 🚀 Всё уже работает!

✅ **ClickHouse** - запущен и настроен
✅ **Backend** - работает на http://localhost:3001
✅ **Frontend** - работает на http://localhost:5173
✅ **Cron** - автоматическая синхронизация каждые 30 минут активна

---

## 📋 Как подключить Яндекс.Директ (Упрощенный способ)

### Шаг 1: Получи токен доступа

1. Открой эту ссылку в браузере:
```
https://oauth.yandex.ru/authorize?response_type=token&client_id=db0084b785964e89908f2b32e246f1de
```

2. Авторизуйся в Яндексе и разреши доступ к API

3. После авторизации тебя перенаправит на страницу с URL типа:
```
https://oauth.yandex.ru/verification_code?dev=True#access_token=y0_AgA...
```

4. **Скопируй токен** - всё что идет после `#access_token=` и до конца строки или до `&`

### Шаг 2: Подключи в системе

1. Открой http://localhost:5173/connect-yandex-simple

2. Заполни форму:
   - **Проект**: выбери существующий или создай новый
   - **Токен доступа**: вставь скопированный токен (начинается с `y0_AgA...`)
   - **Логин**: твой логин в Яндекс.Директ
     - Для агентств: логин клиента (не агентства!)
     - Для прямых рекламодателей: твой логин
   - **ID целей (опционально)**: введи ID целей через запятую, например:
     ```
     252254424, 293622736, 293622699
     ```

3. Нажми **"Подключить Яндекс.Директ"**

### Шаг 3: Смотри аналитику

После подключения:
- Данные автоматически синхронизируются
- Открой http://localhost:5173/yandex-dashboard
- Увидишь красивый дашборд с:
  - 📊 Ключевые метрики (Расход, Клики, Конверсии, ROI)
  - 🎯 **НОВОЕ: Селектор целей конверсий** (выбери конкретную цель или "Все цели")
  - 📈 Таблица всех кампаний с цветовой индикацией
  - 🤖 AI-анализ и рекомендации (если настроен OPENAI_API_KEY)
  - ⏱️ Выбор периода (7/30/90 дней)

---

## 🎯 Что умеет система

### Автоматическая синхронизация
- Каждые 30 минут загружает данные из Яндекс.Директ
- Получает статистику кампаний (показы, клики, расход, конверсии)
- Вычисляет все метрики (CTR, CPC, CR, ROI)
- Сохраняет в ClickHouse для быстрого доступа
- **НОВОЕ:** Загружает детализацию по Device, Age, Gender, IncomeGrade и другим измерениям

### AI-анализ кампаний (опционально)
Если настроен OPENAI_API_KEY, система автоматически выявляет:
- 🔴 **Критические проблемы**: нет конверсий, убыточные кампании
- ⚠️ **Предупреждения**: низкий CTR, высокий CPC, низкая конверсия
- 💡 **Рекомендации**: что исправить и как улучшить

### Красивый дашборд
- Градиентные карточки метрик
- Цветовая индикация (зеленый/красный)
- Стрелки вверх/вниз для ROI
- Статус кампаний (активна/остановлена)
- **НОВОЕ:** Селектор выбора конкретной цели конверсии
  - Выбери "Заявка" - увидишь метрики только по заявкам
  - Выбери "Квалификация" - таблица пересчитается автоматически
  - Выбери "Все цели" - суммарная статистика

---

## 🆕 Новая архитектура с селектором целей

### Проблема старой системы
В старой системе каждая цель конверсии создавала отдельную колонку:
```sql
Conversions_356549152_AUTO
Conversions_350326383_AUTO
Revenue_356549152_AUTO
```

Это делало **невозможным** создание селектора целей - нельзя было динамически переключаться между целями.

### Решение: Нормализованная структура
Теперь конверсии хранятся как отдельные строки:
```sql
goal_id    | conversions | revenue
-----------|-------------|--------
356549152  | 50          | 25000
350326383  | 30          | 15000
```

**Преимущества:**
- ✅ Легко фильтровать: `WHERE goal_id = '356549152'`
- ✅ Селектор работает из коробки
- ✅ Добавление цели = новые строки (не ALTER TABLE!)
- ✅ Гибкая аналитика по любой цели

### Как использовать
1. При подключении **ОБЯЗАТЕЛЬНО укажи ID целей**:
   ```
   252254424, 293622736, 293622699
   ```
2. На дашборде появится селектор целей
3. Выбирай цель - таблица пересчитается автоматически!

**Подробнее:** Смотри [SELECTOR_QUICK_START.md](SELECTOR_QUICK_START.md) и [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)

---

## 🔧 Полезные ссылки

- **Простое подключение**: http://localhost:5173/connect-yandex-simple
- **Дашборд**: http://localhost:5173/yandex-dashboard
- **Главная**: http://localhost:5173
- **API**: http://localhost:3001

---

## ❓ Частые вопросы

### Где найти логин в Яндекс.Директ?
1. Открой https://direct.yandex.ru/
2. В правом верхнем углу будет твой логин
3. Для агентств: логин клиента находится в списке клиентов

### Где найти ID целей?
1. Зайди в настройки кампании в Яндекс.Директ
2. Раздел "Цели и условия"
3. Скопируй ID целей (например: 252254424)
4. Или найди в Яндекс.Метрике в настройках целей

### Токен не работает / Invalid credentials
- Проверь, что скопировал весь токен (начинается с `y0_AgA`)
- Проверь, что логин правильный (для агентств - логин клиента!)
- Попробуй получить новый токен по ссылке выше

### Нет данных на дашборде
- Подожди 1-2 минуты - первая синхронизация идет
- Проверь, что у тебя есть активные кампании в Директе
- Нажми кнопку "Обновить" на дашборде

### Конверсии не отображаются
- Убедись, что в форме подключения указал ID целей
- Проверь, что в кампаниях Директа настроены цели из Метрики
- Проверь, что счетчик Метрики установлен на сайте

---

## 📊 Структура данных в ClickHouse

```bash
# Посмотреть все таблицы
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "SHOW TABLES"

# Посмотреть подключения
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "SELECT login, status, last_sync_at FROM yandex_direct_connections"

# Посмотреть кампании
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "SELECT name, status FROM campaigns LIMIT 10"

# Посмотреть статистику за сегодня
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "SELECT date, sum(clicks), sum(cost) FROM campaign_stats WHERE date = today() GROUP BY date"
```

---

## 🆘 Если что-то сломалось

### Перезапустить сервер
Backend автоматически перезапускается при изменении файлов. Но если нужно:
```bash
cd server
npm run dev
```

### Перезапустить ClickHouse
```bash
docker restart neurodirectolog-clickhouse
```

### Очистить данные и начать заново
```bash
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "TRUNCATE TABLE yandex_direct_connections"
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "TRUNCATE TABLE campaigns"
docker exec neurodirectolog-clickhouse clickhouse-client --database=neurodirectolog --query "TRUNCATE TABLE campaign_stats"
```

---

## 🔑 Опциональная настройка AI-анализа

Чтобы получать AI-рекомендации от GPT-4, добавь в `server/.env`:
```env
OPENAI_API_KEY=your-openai-api-key-here
```

Без этого ключа дашборд будет работать, но AI-рекомендации будут базовыми.

---

## 🎉 Готово!

Теперь у тебя работает полноценная система аналитики Яндекс.Директ:
- ✅ Без заморочек с OAuth приложением в Яндексе
- ✅ Просто токен + логин = работает
- ✅ Красивый дашборд
- ✅ AI-анализ (опционально)
- ✅ Автоматическая синхронизация

**Начни с:** http://localhost:5173/connect-yandex-simple
